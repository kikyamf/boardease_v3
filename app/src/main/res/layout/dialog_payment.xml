<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="//BH NAME QR CODE"
        android:textAlignment="center"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="Scan QR Code to pay for rent."
        android:textAlignment="center"
        android:textSize="14sp" />

    <!-- QR of BH Owner -->
    <ImageView
        android:id="@+id/ivQrCode"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_gravity="center"
        android:scaleType="fitCenter"
        android:src="@drawable/sample_qr_placeholder"/>

    <!-- Proof of Payment -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Proof of Payment:"
        android:textStyle="bold" />
    <ImageView
        android:id="@+id/ivProof"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:src="@drawable/upload"
        android:scaleType="fitCenter"
        android:background="@drawable/edittext_background"
        android:contentDescription="Upload proof"/>

    <!-- DONE button -->
    <Button
        android:id="@+id/btnDone"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="40dp"
        android:backgroundTint="#A18167"
        android:text="DONE"
        android:textColor="@android:color/white"/>
</LinearLayout>
